
a = int(input("Введіть a (додатнє число): "))
while a <= 0:
    a = int(input("Введіть а ще раз (додатнє число): "))

b = int(input("Введіть b (додатнє число): "))
while b <= 0:
    b = int(input("Введіть b ще раз (додатнє число): "))

if a > b:
    x = a * b + 1
elif a == b:
    x = 25
else:
    x = (a - 5) / b


print("Результат обчислення виразу: ", x)
